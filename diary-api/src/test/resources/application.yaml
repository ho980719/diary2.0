spring:
  application:
    name: diary-api
  config:
    import:
      - classpath:application-core.yaml
h2:
  console:
    enabled: true
    path: /h2-console

datasource:
  driver-class-name: org.h2.Driver
  url: jdbc:h2:~/test
  username: sa
  password:

jpa:
  show_sql: true
  hibernate:
    ddl-auto: none
